# 다른 세션 재검 보고서 반영 — 2026-09-03

## 기준과 범위

원 보고서: `HACKATHON_E2E_RETEST_b8f7702_2026-09-03.md`, 증거: `EVIDENCE_b8f7702_2026-09-03.md`.

전달 작업은 **Guide Railway page workflow 2**이며 파일은 다음 위치에서 확인했다.

`/Users/jhungsoohong/Documents/Codex/2026-09-02/referenced-chatgpt-conversation-this-is-an/outputs/`

기준 커밋은 `b8f7702d7392c2899d11a88542a429ee9225f6d5`이다. 앞서 로컬에서 수정한 리뷰 요청·증거 스냅샷을 유지하면서 나머지 항목을 반영했다. 현재 문서는 로컬 코드와 전용 테스트 환경의 결과이며, 라이브 배포 완료나 라이브 전체 재심사 합격을 뜻하지 않는다.

## P1 수정

| 보고서 항목 | 변경 | 검증 |
|---|---|---|
| P1-01 새 리뷰에 옛 값 표시 | 서버가 요청 시점의 실행 증거를 불변 스냅샷으로 저장하고 화면·서명 검증이 같은 증거를 사용한다. 원본 일지와 과거 리뷰는 보존한다. | 원기록 0/true → 담당자 12/false → 새 리뷰 12/false, 메모리·PostgreSQL·화면 확인 |
| P1-02 새 실행에 이전 리뷰 결과 유입 | 세션 액션 이력의 자동 재연결을 제거했다. 완료 알림은 현재 실행의 실제 준비된 단계에만 적용하며 중복·지연 응답이 새 맵을 완료시키지 않는다. | 새로고침 없이 새 실행 시작: completed=[], produced_ids=[], chosen=null, 첫 단계 READY |
| P1-03 request_review 자기 차단 | 요청 생성 시에만 현재 요청 행위를 선행 조건에서 제외한다. 실제 업무·필수 입력은 계속 요구하며, 최종 승인 때에는 요청 완료도 재검한다. | 실제 WebMCP 요청 성공, 실측 이전 요청 거부 회귀 유지 |
| P1-04 삭제 시 검증 조건 유실 | 단일 연결을 우회할 때 앞뒤 조건을 AND로 보존한다. 충돌하는 규칙·다중 분기·자기 루프·중복 경로·입력 담당 소실은 초안을 바꾸기 전에 차단한다. 변경된 연결은 get_map_edits에도 기록한다. | UI로 중간 노드 삭제 후 photoCheckPassed=false 제출 거부, true 허용. 모호한 분기 삭제는 안내 후 원상 유지 |

리뷰 스냅샷·기존 pending 리뷰의 처리 방침은 `REVIEW_EVIDENCE_FIXES_2026-09-03.md`에 설명했다. 기존 리뷰를 추정값으로 채우거나 자동 승인하지 않는다.

새 실행은 새 업무이다. 기존 작업을 계속하려면 **Choose an existing run**에서 그 실행을 선택한다. WebMCP load_process 설명과 README·제출 문서도 이 계약에 맞게 갱신했다.

## P2·P3 반영

- **서명 경계를 넘는 재시도:** task loop와 decision loop 모두 로컬 상태 변경 전에 검사한다. 서명 이후 업무의 정상적인 전진은 허용하며, 복귀 분기를 실제로 선택할 때만 서명 경계가 적용된다. 서버의 기존 서명 보호는 유지한다.
- **결정 대기 표시:** 결정이 남으면 완료 막대 대신 DECISION PENDING, 기록한 작업 수, 아직 경로가 정해지지 않았다는 안내를 표시한다. Next는 실제 연결을 따라 안내하며 미선택 분기의 업무를 임의로 예고하지 않는다.
- **다른 실행의 리뷰 저널:** 대상 runId와 서버에서 읽은 처리 후 상태를 명시한다. 독립 일지는 독립 일지라고 표시한다.
- **실행 선택과 갱신:** Start here·My tasks·Playbooks에 실행 선택기를 제공한다. 열 때 최신 목록을 읽고 수동 새로고침·이전 실행 더 보기를 지원한다. API와 양쪽 저장소에 커서 페이지 조회를 추가했다. 실행을 선택할 때도 최신 단일 실행을 다시 읽어 목록의 오래된 증거를 복원하지 않는다.
- **정확한 새로고침 복원:** 동일 플레이북의 이전 실행을 선택한 뒤 새로고침하면 최신 실행으로 돌아가는 문제를 추가로 재현했다. 탭별 선택 실행 번호를 저장해 같은 실행을 복원하고, 명시적으로 작업을 닫은 상태도 보존한다.
- **버전 비교:** 필드의 추가·삭제·이름·유형·단위·필수 여부·드롭다운 선택지와 분기 조건을 읽기 쉬운 문장과 비교식으로 표시한다.
- **입력 힌트:** 사용자·페르소나·플레이북 버전·단계·필드 유형/단위/선택지로 범위를 제한한다. 출처 실행 번호를 표시하며 현재 값으로 자동 입력하지 않는다. 기존 전역 last 값은 가져오지 않는다.
- 새 실행에서 유지되는 최종 승인 대기와, 후속 업무가 남아 취소될 수 있는 중간 승인 대기를 Playbooks 설명에서 구분한다.

## 검증

### 자동 검사

- 수정 전 SDK 회귀에서 새 실행 오염, 삭제 후 guard 유실, task/decision 서명 경계 문제 **4개 실패를 확인**했다.
- 전용 임시 PostgreSQL DB를 사용한 통합 실행: **59/59 통과**. 해당 실행은 fields.test.mjs를 제외한 당시 모든 테스트 파일을 포함했다.
- 필드 검증 **2/2 통과**.
- 이후 추가한 정확한 실행 복원과 서명 이후 분기 전진을 포함해 SDK/store/입력 힌트/필드 검사 **49/49 통과**.
- 현재 테스트 정의 기준 **63개가 각 검증 실행에서 통과**했다. 63개를 한 번에 실행한 결과라고 표현하지 않는다.
- 메모리·PostgreSQL에서 리뷰 증거, 중간/최종 승인, 변경 증거 취소, 반려·재제출, 서명 불변성, 동시 요청, 응답 손실 복구, 50개 이후 실행 목록 페이지를 검사했다.
- 타입 검사 `npm run check`, 프로덕션 빌드 `npm run build`, `git diff --check` 통과.

### 실제 UI·WebMCP

전용 메모리 서버 `localhost:18996`, 프로덕션 빌드, 데스크톱 화면에서 수행했다.

1. 공개 propose_process_map으로 합성 플레이북을 만들었다. UI에서 중간 중복 노드를 삭제한 뒤 get_process_map·get_map_edits로 연결과 조건 보존을 확인했다.
2. 다중 분기 노드 삭제는 명확한 오류를 표시하고 초안을 보존했다.
3. 저장한 플레이북으로 run #2를 시작했다. 원본 업무 #3은 0/true로 기록했다.
4. 사진 확인 false를 실제 양식으로 제출하면 삭제 전 조건에 의해 거절됐다. true와 현재 실측 12/false, Courier를 제출했다.
5. DECISION PENDING 화면에서 완료 막대·임의의 다음 분기 안내가 사라졌다. Pickup 분기는 evidence_conflict로 거부됐고 Courier 분기만 허용됐다.
6. Park 인계 후 Kim의 명시적 WebMCP request_review가 성공했다. Lee의 review #4에는 12/false와 Courier가 표시됐다. 이 합성 실패값 리뷰는 최종 승인하지 않았다.
7. 새로고침 없이 새 run #5를 열었다. 이전 완료·리뷰 ID·선택 분기가 모두 비어 있고 0개 작업 완료 상태였다.
8. 실행 선택기로 #2를 다시 열었다. 추가 수정 전 새로고침이 #5로 잘못 복원되는 현상을 관찰했고, 수정 후 같은 조작으로 #2와 기존 담당자·시각·12/false가 유지되는 것을 확인했다.
9. 데스크톱 스크린샷으로 실행 선택·승인 대기·타임라인·오른쪽 프로세스 맵을 확인했다.

라이브 run #28/#29와 review #32 등 다른 세션의 증거는 수정·승인·삭제하지 않았다. 브라우저별·물리 모바일·실제 운영 DB 장애 주입 재검은 수행하지 않았다. 새 빌드 배포 후 보고서의 라이브 재검은 별도 단계이다.
