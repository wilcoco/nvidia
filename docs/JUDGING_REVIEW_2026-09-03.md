# Understudy 심사 및 수정 검증 — 2026-09-03

> 후속 상태: 아래 수정은 이후 `93a73e9`로 커밋·푸시됐다. 첫 화면·새 플레이북 생성·재사용·오른쪽 그래픽을 개선한 다음 작업은 [UX 개선·검증 보고서](UX_IMPROVEMENTS_2026-09-03.md)에 기록했다. 아래 점수와 검증 범위는 최초 심사 시점의 기록이다.

검토 기준은 로컬 `7da455b`(직전 코드 변경 `7816808`)와 이 보고서에 기록한 미커밋 수정이다. 점수는 **수정한 로컬 버전에 대한 비공식 모의 심사 82/100**이다. 배포판 점수나 수상 확률을 뜻하지 않는다. 이번 작업에서 커밋·푸시·배포·운영 데이터 초기화는 하지 않았다.

## 심사평

Understudy의 설득력은 작업 기록에서 질문을 만들고, 사람의 답을 프로세스 구조와 증거 입력 항목으로 바꾸며, 다음 실행의 재작업과 승인까지 연결한다는 데 있다. WebMCP가 실제 관찰·질문·수정·실행 인터페이스에 쓰인다. `describe_workspace`, `ask_user`, `get_process_progress`, `resolve_decision`, `run_action`을 중심으로 제품의 핵심 흐름과 도구 사용이 연결되어 있다.

수정 후 확인한 경로에서는 측정값을 바꿨을 때 이전 통과 판정이 무효화되고, 검토 중인 리뷰도 취소되어 새 검토를 요구한다. 승인 결과가 실행 기록에 남고 새로고침 후에도 복원된다. 이 동작은 프로젝트의 주장을 뒷받침한다.

약점은 첫 화면에서 느끼는 제품 정체성과 기능 설명 사이의 거리다. 기본 화면은 업무 일지이며, 핵심 가치는 질문 카드와 옆 패널에서 드러난다. 심사자가 스스로 흐름을 발견하기에는 설명과 상태 표지가 많다. 시연에서는 하나의 구체적인 업무에 집중해 **질문 → 사용자의 교정 → 실패 측정 거부 → 재작업 → 승인**을 한 흐름으로 보여주는 편이 설득력 있다. 실제 도입 사례·절약 시간·사용자 검증 자료는 이번 검토에서 확보하지 못했다.

공식 규정의 네 동등 비중 항목을 각 25점으로 환산했다. 25점 척도 자체와 아래 점수는 이 검토의 판단이다. [공식 심사 기준](https://webmcp.devpost.com/rules)

| 항목 | 점수 | 판단 근거 |
|---|---:|---|
| WebMCP Leverage | 23/25 | 20개 도구가 관찰, 질문, 맵 편집, 진행 판정, 실행에 연결된다. 실제 브라우저 도구 호출로 확인했다. 다른 앱에 적용하려면 의미 있는 액션과 저장소 어댑터 연결이 추가로 필요하다. |
| Execution | 18/25 | 핵심 증거·재작업·승인 경로와 저장을 검증했다. 기존에 상태 오염과 경쟁 조건이 있었고, 이번 패치의 배포 재검 및 최종 시연물 확인은 남아 있다. |
| Potential Impact | 19/25 | 숙련자의 암묵적인 확인 기준을 다음 담당자가 쓸 수 있는 절차로 만드는 문제는 구체적이다. 고객 검증과 정량 효과는 아직 증명되지 않았다. |
| Creativity & Ambition | 22/25 | 관찰과 인터뷰, 사용자의 맵 교정, 실행 시 증거 검사를 연결한 구성이 강점이다. 앱을 넘나드는 실행은 로드맵이므로 현재 성과에 포함하지 않았다. |
| 합계 | **82/100** | 제출 가치가 충분한 데모다. 실운영 보안과 전 시나리오의 완전한 검증을 주장할 단계는 아니다. |

## 다른 세션에서 이어받은 근거

Codex의 다른 작업 **「Guide Railway page workflow 2」**를 읽었다. 해당 세션은 `0282f98` 배포판에서 다음 세 결함을 기록했다.

1. 통과 후 리뷰 대기 중 측정값을 실패값으로 바꾸어도 과거 통과 증거가 남음.
2. 작업형 루프의 두 번째 재시도에서 이전 DONE이 재사용됨.
3. 새 맵 제안·확정 과정에서 이전 실행 기록에 새 맵의 단계가 저장됨.

원본 보고서: `/Users/jhungsoohong/Documents/Codex/2026-09-02/referenced-chatgpt-conversation-this-is-an/outputs/HACKATHON_E2E_RETEST_0282f98_2026-09-03.md`.

이 세 건을 이번 회귀 테스트의 필수 항목으로 삼았다. 이전 세션의 74.3점과 본 점수는 통제된 전후 비교가 아니다. 그 세션의 여러 심사 관점도 이번에 각각 독립 브라우저 실험으로 재현한 것은 아니다. 다른 세션에 메시지를 보내거나 내용을 자동 동기화하지는 않았다.

## 이번에 직접 수정한 주요 결함

| 문제와 영향 | 수정 | 검증 |
|---|---|---|
| 완료한 측정을 다시 열어도 과거 통과 결정이 유효함 | `sdk/mapstore.ts`: 재개한 단계 이후의 결정을 무효화 | SDK 회귀 + 브라우저에서 0 → 재개 → 12, 과거 0으로 통과 요청 시 `evidence_conflict` |
| 검토 중 실행 증거가 바뀌어도 이전 리뷰 승인 가능 | `server/runstate.js`, `server/db.js`: 제출 당시 실행 증거 지문 저장, 변경 시 PENDING 취소, 승인 시 재확인 | 메모리·PostgreSQL 저장소 및 HTTP 회귀 + 브라우저에서 CANCELLED 확인 |
| 자동 생성 검토 기록에 이전 측정값이 남음 | 생성된 draft 기록을 현재 완료 단계의 증거로 갱신; 이전 필드와 검증 표지 제거 | 저장소 테스트에서 `delta=12` 반영, 다음 재시도에서 삭제된 값 제거 |
| 작업형 되돌아가기에서 트리거 작업 DONE 재사용 | 루프 초기화 범위에 되돌아가기를 일으킨 작업도 포함 | 세 번의 재시도마다 fresh work 요구, 선판정 및 가짜 완결 방지 |
| 새 맵이 이전 실행에 저장되거나 늦은 시작 응답이 복구됨 | `sdk/runsync.ts`: 실행과 맵 인스턴스 연결, 중지·복원 세대 가드, 저장 순서 직렬화 | 새 설계의 이전 런 오염 방지; stop/resume 뒤 늦은 응답 무시 |
| 다른 실제 로그인에서 승인하면 실행 동기화 403 | 승인 트랜잭션에서 서버가 관련 실행의 사인오프 반영; 프런트의 타 소유자 런 수정 제거 | judge 로그인 생성 / park 로그인·lee 페르소나 승인, 실행 completed 및 lee 귀속 |
| 지연된 브라우저 저장이 서버 승인 결과를 되돌림 | 서버가 완료된 승인 단계 정보를 보존 | 승인 이후 과거 단계 배열을 저장해도 사인오프와 completed 유지 |
| 동시 제출과 상충 결정이 여러 건 성공 | PostgreSQL 행 잠금·트랜잭션·조건부 UPDATE로 리뷰 생성과 업무 상태 변경을 함께 처리 | 동시 제출 8개 중 1개 성공, APPROVED/REJECTED 경쟁 중 1개 성공 |
| 빈 단계 또는 대체 단계로 서버 검토 관문 우회 | 저장된 프로세스에서 초기 단계 생성; 필수 단계 id/type 제거·교체 및 중복 거부 | 초기 미동기화 제출 409, 대체 단계·null 입력 400 |
| 오래된 자료가 목록 제한 밖으로 사라져 검사가 누락됨 | 핵심 검토·승인·실행 수정 경로는 `getWorklog/getRun/findRunForWorklog` 직접 조회 | 코드 경로 검토; 대량 자료 전체 부하 실험은 미실시 |
| async DB 오류가 Express 응답 처리 밖으로 빠짐 | 모든 async API·인증 미들웨어에 에러 전달 래퍼; ID 범위 검증 | PostgreSQL 오류 응답 뒤 `/api/state` 200; 잘못된 ID 400 |
| 같은 액션 이름의 낡은 승인 카드가 새 맵에서 실행됨 | `sdk/runner.ts`: 맵·확정 상태·역할 및 전제조건 재확인 | 같은 액션을 가진 다른 맵으로 이동 후 실행되지 않음 |
| 최근 액션 자동 연결이 필수 증거 입력을 생략 | 라이브 완료와 동일한 완료 검사를 재사용 | required 증거 없는 과거 액션으로 단계 완료 불가 |
| 폴링 재렌더가 입력 포커스와 조합 중인 문자를 방해 | `sdk/panel.ts`: 텍스트 편집 중 DOM 교체 지연, focusout 후 반영 | 브라우저에서 6초 폴링 동안 값·포커스 유지, Enter 제출 결과 확인 |
| 로그아웃 이후 늦은 실패 응답이 세션 처리를 다시 실행 | `src/store.ts`: refresh 실패 경로에도 세대 가드 | 코드 경로 검토; 로그아웃 지연 네트워크 E2E는 미실시 |
| 사용자별 seed 존재 검사와 삽입 사이 경쟁 | 기본 계정 `INSERT ... ON CONFLICT DO NOTHING` | 메모리·PostgreSQL 4개 기본 사용자 확인 |

기존 Claude 수정 중 저장 실패 시 draft 복귀, 비활성 분기 수동 체크 방지도 회귀 테스트에 포함했다. 이전 감사 15건 전부에 대해 이번 브라우저 실험을 새로 수행했다는 뜻은 아니다.

## 실행한 검증

2026-09-03 13:59 KST 자동 테스트가 통과했고, 마지막 키보드 편집 보완 뒤 타입 검사·빌드를 다시 통과했다.

```sh
npm run check
npm run build
TEST_DATABASE_URL=postgresql://review@127.0.0.1:18988/postgres npm test
git diff --check
```

**자동 테스트 12/12 통과**: SDK 8개, 메모리·PostgreSQL HTTP 각 1개, 메모리·PostgreSQL 저장소 각 1개. 하나의 테스트 안에 여러 상태 전이와 병행 요청 검사가 포함되어 있다. 기본 `npm test`는 외부 DB 없이 10개를 실행한다. PostgreSQL까지 검사하려면 `TEST_DATABASE_URL`을 별도 테스트 DB로 지정해야 한다. 테스트는 해당 DB에 자료를 생성하므로 운영 DB를 지정하지 않는다.

실제 브라우저·WebMCP 실험은 별도 로컬 메모리 서버 `localhost:18987`에서 수행했다. 질문 입력 → 응답 읽기 → 맵 생성/역할·필드 설정 → UI 확정·저장 → 실행 → 실패 증거 거부 → 재작업 → 새 리뷰 → 승인 → 새로고침 후 완료 복원을 확인했다. 전체 UI 흐름은 마지막 서버 증거 갱신 보완 직전에 수행했고, 그 보완은 위 최종 PostgreSQL/메모리 테스트로 확인했다. 마지막 UI 빌드에서는 별도 탭에서 단계 이름 편집의 Enter 확정·Escape 취소도 확인했다. 한글 IME 조합 중 Enter는 제출하지 않도록 코드에 가드를 넣었으나 실제 IME 입력 실험은 하지 않았다. 운영 배포판에 같은 패치를 적용해 수행한 E2E는 아니다.

## 남은 한계와 제출 우선순위

1. **서버가 모든 업무 증거의 진위를 판정하는 구조는 아니다.** 실행 단계의 id/type과 리뷰 전제는 검증하지만, 단계 상태·결정·측정값은 소유 클라이언트가 제출한다. 악의적인 API 사용자를 상대로 한 완전한 증거 검증은 서버에서 저장된 그래프와 액션 결과로 진행을 재계산하는 별도 작업이 필요하다. 실제 사용자 인증에 역할을 결합하는 작업도 필요하다. 현재 페르소나 전환은 데모 모델이다.
2. 승인 지문 도입 전의 **기존 PENDING 리뷰는 새 검토가 필요하다.** 지문 없는 실행 연결 리뷰는 승인할 수 없고, 실행 동기화 시 취소되어 draft에서 재요청한다. 배포 후 시연에는 새 실행으로 만든 리뷰를 사용한다.
3. 최신 목록 제한은 핵심 승인 검사에서 제거했지만, 오래된 실행의 일괄 정리·취소 검색에는 제한된 목록을 사용하는 코드가 남아 있다. 별도 로그인 브라우저 두 개의 실시간 화면 수렴, 다중 탭 동시 편집, 모바일 전체 흐름, 13개 플레이북 전수 회귀는 이번 검증 범위 밖이다.
4. 설명의 신뢰도를 높이기 위해 README·제출문·특장점 문서에서 “다른 방식으로는 불가능”, “승인 클릭은 위조 불가”, “사용량 제한 없음” 같은 과도한 주장을 완화했다. WebMCP 인터페이스의 확인 절차와 실제 인증 경계를 구분했다.
5. **제출 준비 완료 여부는 아직 확인하지 않았다.** 인수 문서상 영상은 미촬영 상태였다. 공식 규정에 맞는 3분 미만 공개 YouTube 시연, 음성 설명, 영어 또는 영어 번역, 실행 가능한 링크와 저장소 접근을 제출 전에 확인해야 한다. 마감은 2026-09-04 05:00 KST다. [공식 제출 규정](https://webmcp.devpost.com/rules)

권장 다음 순서는 이 패치 검토·배포, 새 실행으로 증거 변경/재작업/승인/복원을 짧게 재검, 한 업무 중심 영상 제작, 제출물 확인이다. 현재 시연용 플레이북과 운영 데이터는 보존했다.
